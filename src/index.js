import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Renders the contents of 'App' at 'root'
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
